# Mnemostroma: Feedback Layer

This module tracks and processes implicit and explicit human feedback to refine the agent's behavior.

## Components
- `ImplicitFeedbackTracker`: Monitors interaction signals to adjust `ram_index` priorities.
