# Аудит_Клауде_Мнемострома — 2026-04-01

> Полный технический аудит состояния проекта.  
> Источники: исходный код, git-история, Context Transfer v3/v4, документация, архивные стратегические файлы.

---

## 0. СНИМОК СОСТОЯНИЯ

| Параметр | Значение |
|---|---|
| Версия | v1.6.2 (CHANGELOG) |
| Тесты | **79/79 pass** ✅ |
| Ветка | `main` (активная) |
| Коммитов не запушено | **17** (⚠️ критический риск) |
| RAM (подтверждено E2E) | 631 MB (бюджет 700 MB) |
| БД | mnemostroma.db dim=384, WAL |

---

## 1. АРХИТЕКТУРА — ФАКТИЧЕСКОЕ СОСТОЯНИЕ

### Реализованные модули (main, 2026-03-24 → 2026-03-31)

| Модуль | Файл(ы) | Статус |
|---|---|---|
| Conductor (bootstrap + orchestration) | conductor.py | ✅ полный |
| SystemContext + ModelRegistry | core.py | ✅ полный |
| Config | config.py | ✅ |
| Observer pipeline | observer/pipeline.py | ✅ (B.2/B.3 TODO) |
| HybridNER (DistilBERT + regex) | models/hybrid_ner.py | ✅ |
| BertNER | models/bert_ner.py | ✅ |
| NERObserver | models/ner_observer.py | ✅ |
| ONNX Engine + EnginePool | models/onnx_engine.py, engine_pool.py | ✅ |
| Reranker | models/reranker.py | ✅ (интеграция с dim=384 не верифицирована) |
| Flag Detector (B.1) | observer/flag_detector.py | ✅ 22 теста |
| HNSW (session + content) | memory/hnsw.py | ✅ |
| Session Index | memory/session_index.py | ✅ |
| Dissolver | memory/dissolver.py | ✅ |
| Scoring | memory/scoring.py | ✅ |
| Search | memory/search.py | ✅ |
| Reranker (memory) | memory/reranker.py | ✅ |
| Consolidation Worker | memory/consolidation.py | ✅ |
| Anchor Layer | subconscious/anchor.py, anchor_index.py | ✅ |
| SQLite + DatabaseManager | storage/sqlite.py | ✅ |
| ContentManager | storage/content_manager.py | ✅ |
| LogWriter | storage/log_writer.py | ✅ |
| ImplicitFeedback | feedback/implicit.py | ✅ |
| Conflict Detector | tuner/conflict.py | ✅ |
| Drift Detector | tuner/drift.py | ✅ |
| MCP Server (stdio) | integration/mcp_server.py | ✅ 280 строк |
| Conductor Proxy | integration/proxy.py | ✅ |
| Tools: admin, read, write | tools/ | ✅ |

### НЕ реализовано (подтверждено Context Transfer v4 + код)

| Функционал | Этап | Приоритет |
|---|---|---|
| `continuation_detector.py` | B.2 | **ВЫСОКИЙ** — архитектура согласована, код не написан |
| `mention_type` (focus vs passing) | B.3 | средний |
| Decay Engine | C | средний |
| Dreamer | D | низкий |
| `ctx.bridge()` / Session Bridge | — | средний |
| Experience Layer (собранный файл) | — | средний |
| Onboarding Pipeline | — | средний |
| `safe` / `debug` режимы логирования | — | **ВЫСОКИЙ** (блокер alpha) |
| `mnemostroma install-models` CLI | — | **ВЫСОКИЙ** (блокер публикации) |

---

## 2. ТЕХНИЧЕСКИЕ ДОЛГИ

### TD-01 — 17 коммитов не запушены ⛔ КРИТИЧНО
- `main` опережает `origin/main` на 17 коммитов
- Потеря машины = потеря всей работы с 2026-03-24 по 2026-03-31
- **Действие**: `git push origin main` немедленно

### TD-02 — Устаревшая документация (MAJOR GAP)
- `INDEX_v4.md` (2026-03-25) утверждает: *"v1.x MVP реализация не начата"*
- `MNEMOSTROMA_TODO_v2.md` таблица: все слои помечены `⬜ не начата`
- Реальность: 17 коммитов, v1.6.2, 79 тестов, ~80% MVP реализовано
- **Действие**: обновить INDEX, TODO, CHANGELOG под текущее состояние

### TD-03 — Мусорные файлы в репозитории
| Файл | Проблема |
|---|---|
| `src/mnemostroma/core.py.old` | старая версия core в репозитории |
| `src/mnemostroma/models/__init__.py.old` | устаревший init |
| `src/mnemostroma/models/pure_gliner.py` | deprecated, заменён HybridNER |
| `check_paths.py` в корне | диагностический скрипт в main |
| `test_touch.txt` в корне | тестовый артефакт |
| `_backup_refactor_20260329_222354/` | резервная копия в корне репо |

### TD-04 — Неиспользуемые модели (~1GB+ диска)
По `models_manifest.json` активны только: `multilingual-e5-small`, `distilbert-ner`, `tinybert-l2-v2`.

Неиспользуемые папки в `models/`:
- `gliner-small-v2.1/` — deprecated, заменён HybridNER
- `gte-multilingual-base/` — заменён E5-small
- `distiluse-base-multilingual-v2-merged-onnx/` — нигде не упоминается
- `Xenova-multilingual-e5-small/` — дубль `multilingual-e5-small/`

### TD-05 — TODO в коде (незакрытые B.2/B.3)
```
observer/pipeline.py:166 — "is_new_entity": True,  # TODO: Phase B.2 — cosine check
observer/pipeline.py:167 — "continuation_of": None, # TODO: Phase B.2
observer/pipeline.py:168 — "continuation_depth": 0, # TODO: Phase B.2
observer/pipeline.py:169 — "mention_type": "focus", # TODO: Phase B.3
models/embedding_utils.py:26 — # TODO: AST-aware splitting for code
```
Значения захардкожены — anchor.flags содержит некорректные placeholder-данные.

### TD-06 — Reranker интеграция не верифицирована
- Context Transfer v4: *"Reranker интеграция с E5-small dim=384 — не проверена"*
- `tinybert-l2-v2` lazy loaded, нет E2E теста полного пайплайна с reranker

### TD-07 — RAM health check placeholder в conductor
```python
# conductor.py — Bootstrap Log Point #17
"ram_mb": 0.0,  # Placeholder for psutil logic
```
health check логирует 0 вместо реального потребления RAM.

### TD-08 — Нет safe/debug режима логирования
- `logs.db` пишет 18 типов событий по умолчанию
- Нет флага для отключения / уменьшения логирования
- Критично для public alpha: продукт позиционируется offline/private, а логи не объяснены пользователю
- **Блокер для alpha-раздачи** (см. стратегические файлы в архиве)

---

## 3. ГАПЫ В РЕАЛИЗАЦИИ

### GAP-01 — Спека B.2 готова, код не написан
- Файл `Новый_модуль_observer_continuation_detector.py.md` в архиве содержит полную архитектуру
- Согласованное решение: V3 scoring (cosine×0.7 + tags×0.1 + recency×0.2), threshold=0.65
- Нужно создать: `observer/continuation_detector.py` + тесты

### GAP-02 — Experience Layer не собран
- Материал существует в 4+ файлах (SESSION_BRIDGE, ADR_001, config_tuner_patch)
- Единого `experience_layer_specification_v1.0.md` нет
- ExperienceCluster, intuition_signal, +/- Experience Index — нигде не реализованы

### GAP-03 — MCP инструменты неполные
- `ctx.semantic()`, `ctx.active()` в read.py — есть
- `ctx.bridge()` — не реализован
- Полный список из api_tools_specification v1.3 (18+ инструментов) — покрытие не проверено

### GAP-04 — `mnemostroma install-models` не существует
- Стратегический план требует: `pip install mnemostroma` → `mnemostroma install-models`
- Никакого CLI install-скрипта нет
- **Блокер для публичного GitHub** (пользователь не сможет запустить)

### GAP-05 — Deployment Guide vs реальность
- `deployment_guide.md` описывает установку, но нет `Makefile`, нет `mnemostroma` CLI entrypoint
- `pyproject.toml` — нужно проверить scripts/entrypoints

---

## 4. ХВОСТЫ И ТРЕНИЯ

### HV-01 — `core.py` vs `core.py.old` — неясно что актуально
- Оба файла в репозитории, нет комментария зачем `.old` оставлен

### HV-02 — Два пути к E5-small модели
- `Xenova-multilingual-e5-small/` (Context Transfer v3 упоминает этот путь)
- `multilingual-e5-small/` (manifest указывает этот)
- В Context Transfer v3: `models/Xenova-multilingual-e5-small/onnx/model_int8.onnx`
- В manifest и v4: `models/multilingual-e5-small/onnx/model_int8.onnx`
- Неясно, это переименование или два разных файла

### HV-03 — `mcp__mnemostroma` активный контекст содержит тестовые данные
- `ctx_active` показывает: `"Иван Петров из Яндекса решил использовать PostgreS"`
- Это тестовые данные из `test_basic.py` — демон работал с тестовыми записями
- Реальные рабочие данные в БД смешаны с тестовыми сессиями

### HV-04 — Dim migration 768→384 оставила backup файлы
- `mnemostroma.db.bak_768` — стала ненужной после миграции
- `.db-shm` и `.db-wal` файлы не в .gitignore (хотя не добавлены вручную)

### HV-05 — `_backup_refactor_20260329_222354/` в корне
- Содержит: `core.py`, `memory/search.py`, `models/__init__.py`, `observer/pipeline.py`, `storage/content_manager.py`
- Это snapshot перед рефактором от 29.03 — полезен как reference, но засоряет корень

### HV-06 — Alpha ветка расходится с main без ясной политики
- `alpha` ветка: остановилась на коммите `07af017` (до большинства фич)
- Нет документа о том, что такое `alpha` и как синхронизируется с `main`
- В планах нужен чистый public repo — вероятно `alpha` = public branch, но нигде не зафиксировано

---

## 5. НЕОБОСНОВАННЫЕ / НЕПОНЯТНЫЕ АРХИТЕКТУРНЫЕ ОТСТУПЛЕНИЯ

> Этот раздел — отдельно, как было запрошено. Здесь только то, что **не имеет объяснения в доступных документах**.

### ❓ АО-01 — `distiluse-base-multilingual-v2-merged-onnx/` в models/
- Нет упоминания в `models_manifest.json`
- Нет упоминания в Context Transfer v3/v4
- Нет упоминания в specs
- Неясно: это экспериментальный артефакт, старый embedder или запасной вариант?

### ❓ АО-02 — `models/gte-multilingual-base/` остался после замены на E5-small
- В CHANGELOG v1.6.0 (2026-03-29): переход на shared GTE-768d
- В CHANGELOG после: переход на E5-small 384d
- GTE папка осталась, не удалена, не закомментирована в manifest
- Неясно: намеренно оставлена как fallback или просто забыта?

### ❓ АО-03 — `audit_log_calls.txt` в archive
- Текстовый файл с аудитом вызовов — непонятного происхождения
- Не referenced ни в одном документе

### ❓ АО-04 — `read.md`, `scoring.md`, `search.py.md`, `session_embedder.py.md` в archive
- Markdown-файлы, описывающие код (по имени — документация модулей)
- Неясно: это генерированные описания удалённых файлов или живые документы?

### ❓ АО-05 — `walkthrough29-03-2-14-10.md` и `walkthrough29-03.md` в archive
- Два похожих файла с разным временем (2:14:10 и без времени)
- Дублирование или разные версии?

---

## 6. СТРАТЕГИЧЕСКИЙ КОНТЕКСТ (из архива)

> Найдено в `archive of documents/Cosed_docs_git_ignore/` — влияет на приоритеты.

**Основная цель (зафиксированная):** запустить `mnemostroma.my` + публичный GitHub core repo.

**Критические блокеры запуска:**

| Блокер | Статус |
|---|---|
| `mnemostroma install-models` CLI | ❌ не существует |
| Safe/default режим логирования | ❌ не реализован |
| Публичный GitHub (clean public repo) | ⚠️ alpha ветка устарела, не синхронизирована |
| 17 коммитов не запушены | ⛔ катастрофический риск |
| README статус ("tested core, alpha") | ⚠️ README не обновлён под реальное состояние |

**Модели — операционный риск:**
- Стратегические документы упоминают `EmbeddingGemma-300m` как основной embedder
- Реальный код использует `multilingual-e5-small` (manifest, Context Transfer v4)
- EmbeddingGemma требует HF license acceptance — это упоминается как риск в архиве
- **Расхождение**: strategic docs vs. actual implementation в части стека моделей

---

## 7. СОСТОЯНИЕ ВЕТОК GIT

| Ветка | Последний коммит | Описание | Рекомендация |
|---|---|---|---|
| `main` | `fd26cde` fix: brief truncation | Активная разработка, v1.6.2 | Запушить немедленно |
| `alpha` | `07af017` refactor: Config-Driven | Остановилась до HybridNER, B.1 | Нужна политика синхронизации |
| `backup/pre-split-20260328` | snapshot 28.03 | Backup перед рефактором | Можно удалить после push |
| `origin/main` | 17 коммитов позади | Удалённый репозиторий | Устарел |

---

## 8. ТЕСТЫ

| Метрика | Значение |
|---|---|
| Всего | 79 |
| Passing | 79 ✅ |
| Failing | 0 |
| Покрытие B.2 (continuation_detector) | 0% (модуль не создан) |
| Покрытие B.3 (mention_type) | 0% |
| Покрытие Decay Engine | 0% |
| E2E с reranker (dim=384) | не верифицировано |

---

## 9. ПРИОРИТИЗИРОВАННЫЙ СПИСОК ДЕЙСТВИЙ

### НЕМЕДЛЕННО (блокеры)

1. **`git push origin main`** — 17 коммитов под угрозой потери
2. **Safe logging mode** — `logging.enabled: false/true` в config.json — блокер alpha-раздачи
3. **`mnemostroma install-models`** CLI — блокер публичного запуска

### КОРОТКИЙ СРОК (до публикации)

4. Реализовать `continuation_detector.py` (B.2) — архитектура готова в архиве
5. Удалить/gitignore: `core.py.old`, `__init__.py.old`, `pure_gliner.py`, `check_paths.py`, `test_touch.txt`
6. Убрать `_backup_refactor_20260329_222354/` из корня (в gitignore или удалить)
7. Разобраться с `Xenova-multilingual-e5-small/` vs `multilingual-e5-small/` — один путь
8. Обновить `INDEX_v4.md` и `MNEMOSTROMA_TODO_v2.md` под реальное состояние v1.6.2
9. Прояснить политику ветки `alpha` (= public branch?)

### СРЕДНИЙ СРОК

10. Исправить health check placeholder в conductor.py (TD-07)
11. Верифицировать Reranker с dim=384 (E2E тест)
12. Удалить неиспользуемые модели из `models/` (~1GB)
13. Собрать `experience_layer_specification_v1.0.md`
14. Реализовать B.3 mention_type

---

## 10. ИТОГОВАЯ ОЦЕНКА

**Техническое состояние ядра:** сильное. 79/79 тестов, реализованы все компоненты Observer → Storage → HNSW → Conductor → MCP. Архитектура выдержана.

**Главный риск:** 17 коммитов не запушены — это не технический долг, это операционная угроза всему проекту.

**Главный гап между кодом и планом запуска:** нет install CLI + нет safe logging mode = публичный GitHub без возможности установки и с privacy-риском.

**Документация:** отстаёт от реализации на ~1 месяц. INDEX, TODO, WEAK_POINTS написаны до большинства реализации — вводят в заблуждение при переносе контекста.

---

*Аудит проведён: 2026-04-01 | Claude Sonnet 4.6 | Источники: src/, tests/, git log, Context Transfer v3/v4, docs, archive/*
