# examples/

End-to-end usage examples for common integration scenarios.

Planned:
- `basic_mcp/` — Minimal MCP server setup with Claude Desktop
- `claude_ai/` — Connecting to claude.ai via SSE
- `custom_agent/` — Integrating Mnemostroma into a custom Python agent loop
- `content_branch/` — Using the Content Branch for versioned artifact management
